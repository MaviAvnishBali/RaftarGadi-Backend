name: core
description: Raftar Gaddi shared package — core.
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.9.0
  flutter: ">=3.44.0"

resolution: workspace

dependencies:
  flutter:
    sdk: flutter
  flutter_riverpod: ^3.3.2
  fpdart: ^1.2.0
  riverpod_annotation: ^4.0.3
  shared_models:
    path: ../shared_models
  api_client:
    path: ../api_client

dev_dependencies:
  build_runner: ^2.15.1
  flutter_test:
    sdk: flutter
  riverpod_generator: ^4.0.4
