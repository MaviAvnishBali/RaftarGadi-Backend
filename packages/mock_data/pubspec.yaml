name: mock_data
description: Raftar Gaddi — in-memory fake API data source (fixtures + route handlers).
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.9.0
  flutter: ">=3.44.0"

resolution: workspace

dependencies:
  core:
    path: ../core
  flutter:
    sdk: flutter
  shared_models:
    path: ../shared_models

dev_dependencies:
  flutter_test:
    sdk: flutter
