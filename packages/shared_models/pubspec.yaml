name: shared_models
description: Raftar Gaddi shared package — shared_models.
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.9.0

resolution: workspace

dev_dependencies:
  build_runner: ^2.15.1
  freezed: ^3.2.6-dev.1
  json_serializable: ^6.14.0
  test: ^1.25.0
dependencies:
  freezed_annotation: ^3.1.0
  json_annotation: ^4.12.0
  meta: ^1.18.0
